# 敏感配置文件
# application*.yml 一把梭：application.yml 里含 JWT 密钥，必须挡住。
# 仓库里只保留 application.yml.example，克隆后照着复制改名即可。
# （此前只写 application-*.yml 匹配不到 application.yml，密钥曾进过 git 历史）
application*.yml
!application*.yml.example
*.properties

# 环境变量文件
.env
.env.local

# IDE 配置
.idea/
.vscode/
*.iml
*.iws
*.ipr

# Maven 编译产物
target/
**/target/
*.jar
*.war

# 日志文件
logs/
*.log

# 操作系统文件
.DS_Store
Thumbs.db

# 临时文件
*.tmp
*.swp
*~

# AI 工具运行时缓存
.reasonix/

# AI 助手工作目录（含对话沉淀、环境记录、演练笔记，勿提交）
.workbuddy/

# JMeter 压测产物（HTML 报告体积大，CSV 为本地运行输出）
bench/*_report/
bench/**/*.csv
bench4_*.csv

# 本地启动脚本（含 API Key，勿提交）
start-with-env.bat
